#=======================================================================================
#.       tui_run.py — TUI 控制台模式入口
#.       创建 BotTUI 应用实例并启动 Textual 终端界面。
#.       TUI 模式下 Bot 在后台运行，用户可通过界面管理配置、
#.       开关功能、查看日志和会话历史。
#=======================================================================================

# -- BotTUI 位于 tui/app.py，是 Textual 主应用类
from tui.app import BotTUI

if __name__ == "__main__":
    app = BotTUI()
    app.run()
